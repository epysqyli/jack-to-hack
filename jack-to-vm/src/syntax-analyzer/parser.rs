// The parser:
// - takes in an input stream of tokens
// - recursively applies a set of grammar rules to it
// - produces a derivation/parse tree or fails if grammer rules are not applicable

use crate::syntax_analyzer::tokenizer::Token;

pub fn parse(tokens: Vec<Token>) {
    // TODO: implement
}
